<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Movimiento rps</h1>
    <%= link_to 'New movimiento rp', new_movimiento_rp_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <div id="movimiento_rps" class="min-w-full">
    <%= render @movimiento_rps %>
  </div>
</div>
